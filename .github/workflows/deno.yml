name: Validate changes

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

env:
  ZEPHYR_TOKEN: ${{ secrets.ZEPHYR_TOKEN }}
  JIRA_PAT: ${{ secrets.JIRA_PAT }}

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Setup repo
        uses: actions/checkout@v3

      - uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x

      - name: Verify formatting
        run: deno task check

      - name: Validate changes
        run: deno task validate

      - name: Validate All
        run: deno task validate-all

      - name: Check spellings
        uses: codespell-project/actions-codespell@master
        with:
          ignore_words_file: .codespellignore
          path: data

      - name: Get branch names
        id: branch-name
        uses: tj-actions/branch-names@v6

      - name: Save changes
        if: steps.branch-name.outputs.is_default == 'false'
        run: deno task save

      - name: Sync changes
        if: steps.branch-name.outputs.is_default == 'false'
        run: deno task sync

      # - uses: stefanzweifel/git-auto-commit-action@v4
      #   if: steps.branch-name.outputs.is_default == 'false'
      #   with:
      #     commit_message: "chore(data): Sync"
      #     create_branch: true
      #     branch: data-sync
      #     add_options: '-u'
      #     skip_dirty_check: true
      #     commit_user_name: mattermost-quality
      #     commit_user_email: qa@mattermost.com


      # - name: Create Pull Request
      #   if: steps.branch-name.outputs.is_default == 'false'
      #   uses: peter-evans/create-pull-request@v3
      #   with:
      #     token: ${{ secrets.GITHUB_TOKEN }}
      #     commit-message: 'Sync(data)'
      #     title: 'Update changes to Test cases'
      #     body: 'Pull request to update data from Zephyr.'
      #     branch: 'data-sync'
      #     base: 'main'
      #     committer: 'mattermost-quality'
      #     author: 'qa@mattermost.com'

      - name: Run the Action
        uses: devops-infra/action-pull-request@v0.5.5
        with:
          github_token: ${{ secrets.TOKEN }}
          source_branch: data-sync
          target_branch: main
          title: My pull request
          body: "**Pull request to update data from Zephyr**"
          reviewer: mattermost-quality
          assignee: mattermost-quality
          draft: true
          get_diff: true
          allow_no_diff: false
